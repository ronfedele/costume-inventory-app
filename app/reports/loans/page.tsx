import { PageHeader } from "@/components/PageHeader";

export default function ReportPage() {
  return (
    <div>
      <PageHeader title="Loan Reports" description="Due-soon, overdue, returned, damaged, and fee summary reports." />
      <div className="card"><p>TODO_SUPABASE: Replace with filters, export buttons, and printable output.</p></div>
    </div>
  );
}
