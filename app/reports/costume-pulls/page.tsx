import { PageHeader } from "@/components/PageHeader";

export default function ReportPage() {
  return (
    <div>
      <PageHeader title="Costume Pull Reports" description="Printable costume pull lists by production, character, or performer." />
      <div className="card"><p>TODO_SUPABASE: Replace with filters, export buttons, and printable output.</p></div>
    </div>
  );
}
