import { PageHeader } from "@/components/PageHeader";

export default function ReportPage() {
  return (
    <div>
      <PageHeader title="Inventory Reports" description="Per-site, per-location, and discrepancy inventory reports." />
      <div className="card"><p>TODO_SUPABASE: Replace with filters, export buttons, and printable output.</p></div>
    </div>
  );
}
