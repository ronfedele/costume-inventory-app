import { PageShell } from "@/components/PageShell";

export default function ReportFittingsPage() {
  return (
    <PageShell title="Fitting Reports" description="Fitting status by production and performer.">
      <p>This becomes fully functional after fitting tables are added.</p>
    </PageShell>
  );
}
