import { PageShell } from "@/components/PageShell";

export default function PropsPage() {
  return (
    <PageShell title="Props & Set Pieces" description="Starter page for props and scenic items.">
      <p>Connect this page to asset + prop_set_item filters.</p>
    </PageShell>
  );
}
