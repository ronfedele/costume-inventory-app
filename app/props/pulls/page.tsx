import { PageHeader } from "@/components/PageHeader";

export default function PropPullsPage() {
  return (
    <div>
      <PageHeader title="Prop Pull Lists" description="Generate and print production prop pulls." />
      <div className="card"><p>TODO_SUPABASE: Filter by production, scene, availability, and location.</p></div>
    </div>
  );
}
