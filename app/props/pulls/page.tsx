import { PageShell } from "@/components/PageShell";

export default function PropPullsPage() {
  return (
    <PageShell title="Prop Pulls" description="Printable and sortable pull list page.">
      <p>Generate prop pull lists by production and scene.</p>
    </PageShell>
  );
}
