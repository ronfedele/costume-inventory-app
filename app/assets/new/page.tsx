import { PageShell } from "@/components/PageShell";

export default function NewAssetPage() {
  return (
    <PageShell title="New Asset" description="Starter page for asset intake.">
      <p>Next step: add a form that saves to the asset table and any type-specific child table.</p>
    </PageShell>
  );
}
