import { PageShell } from "@/components/PageShell";

export default function AdminRolesPage() {
  return (
    <PageShell title="Roles & Permissions" description="Manage role definitions and assignments.">
      <p>Connect this page to role, permission, role_permission, and user_site_role.</p>
    </PageShell>
  );
}
