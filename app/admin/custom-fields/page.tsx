import { PageShell } from "@/components/PageShell";

export default function AdminCustomFieldsPage() {
  return (
    <PageShell title="Custom Fields" description="Enable site-specific custom fields.">
      <p>Connect this page to custom_field_definition and custom_field_value.</p>
    </PageShell>
  );
}
