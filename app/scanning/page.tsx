import { PageShell } from "@/components/PageShell";

export default function ScanningPage() {
  return (
    <PageShell title="Labels & Scanning" description="Barcode and QR workflows.">
      <p>Use asset_tag here now. Add scan session tables for full audit workflows.</p>
    </PageShell>
  );
}
