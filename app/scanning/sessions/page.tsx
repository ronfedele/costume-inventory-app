import { PageShell } from "@/components/PageShell";

export default function ScanSessionsPage() {
  return (
    <PageShell title="Scan Sessions" description="Inventory audit sessions and exceptions.">
      <p>Create inventory_scan_session and inventory_scan_event to power this page.</p>
    </PageShell>
  );
}
