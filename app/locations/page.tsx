import { PageShell } from "@/components/PageShell";

export default function LocationsPage() {
  return (
    <PageShell title="Storage Map" description="Hierarchical storage location browser.">
      <p>Connect this to storage_location and asset_location_history.</p>
    </PageShell>
  );
}
